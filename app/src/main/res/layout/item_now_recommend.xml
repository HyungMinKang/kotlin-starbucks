<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="productImage"
            type="String" />
        <variable
            name="productTitle"
            type="String" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="15dp"
        >

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/iv_now_recommend_menu"
            android:layout_width="120dp"
            android:layout_height="120dp"
            getImage="@{productImage}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            />

        <TextView
            android:id="@+id/tv_now_recommend_menu"
            android:layout_width="wrap_content"
            android:textSize="8dp"
            style="@style/TextSmall.Bold"
            android:layout_marginTop="5dp"
            android:textAlignment="center"
            android:layout_height="wrap_content"
            android:text="@{productTitle}"
            app:layout_constraintStart_toStartOf="@id/iv_now_recommend_menu"
            app:layout_constraintEnd_toEndOf="@id/iv_now_recommend_menu"
            app:layout_constraintTop_toBottomOf="@id/iv_now_recommend_menu" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>